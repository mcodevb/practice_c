#include<stdio.h>
int main()
{
    int n, num, indx, isprime, prime[10];
    
    prime[0] = 2;
    indx = 1;
    num = 3;

    while(indx<10)
    {
        // check if num is prime or not

        // if num is prime assign it to prime[indx] and increase indx by 1
        // otherwise just increase num by 1 and go to next iteration of the loop
    }



}